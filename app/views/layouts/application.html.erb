<!DOCTYPE html>
<html>
  <head>
    <title>BudgetApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if notice %>
      <p style="position: absolute; bottom: 1rem; width: 90%; left: 5%;" class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p style="position: absolute; bottom: 1rem; width: 90%; left: 5%;" class="alert alert-danger"><%= alert %></p>
    <% end %>
    <%= yield %>
  </body>
</html>
